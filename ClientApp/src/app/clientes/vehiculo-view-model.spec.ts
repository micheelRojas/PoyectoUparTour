import { VehiculoViewModel } from './vehiculo-view-model';

describe('VehiculoViewModel', () => {
  it('should create an instance', () => {
    expect(new VehiculoViewModel()).toBeTruthy();
  });
});
