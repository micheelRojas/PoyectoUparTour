import { RestauranteViewModel } from './restaurante-view-model';

describe('RestauranteViewModel', () => {
  it('should create an instance', () => {
    expect(new RestauranteViewModel()).toBeTruthy();
  });
});
