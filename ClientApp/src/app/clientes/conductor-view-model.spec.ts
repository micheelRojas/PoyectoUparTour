import { ConductorViewModel } from './conductor-view-model';

describe('ConductorViewModel', () => {
  it('should create an instance', () => {
    expect(new ConductorViewModel()).toBeTruthy();
  });
});
