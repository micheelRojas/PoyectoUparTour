import { TransporteViewModel } from './transporte-view-model';

describe('TransporteViewModel', () => {
  it('should create an instance', () => {
    expect(new TransporteViewModel()).toBeTruthy();
  });
});
