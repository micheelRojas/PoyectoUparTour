import { FiltroHotelPipe } from './filtro-hotel.pipe';

describe('FiltroHotelPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroHotelPipe();
    expect(pipe).toBeTruthy();
  });
});
