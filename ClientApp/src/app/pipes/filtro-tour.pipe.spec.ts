import { FiltroTourPipe } from './filtro-tour.pipe';

describe('FiltroTourPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTourPipe();
    expect(pipe).toBeTruthy();
  });
});
