import { FiltroLugarPipe } from './filtro-lugar.pipe';

describe('FiltroLugarPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroLugarPipe();
    expect(pipe).toBeTruthy();
  });
});
