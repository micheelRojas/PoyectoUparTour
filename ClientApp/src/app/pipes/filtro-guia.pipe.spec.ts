import { FiltroGuiaPipe } from './filtro-guia.pipe';

describe('FiltroGuiaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroGuiaPipe();
    expect(pipe).toBeTruthy();
  });
});
