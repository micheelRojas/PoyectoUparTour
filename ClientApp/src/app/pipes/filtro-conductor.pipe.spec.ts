import { FiltroConductorPipe } from './filtro-conductor.pipe';

describe('FiltroConductorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroConductorPipe();
    expect(pipe).toBeTruthy();
  });
});
