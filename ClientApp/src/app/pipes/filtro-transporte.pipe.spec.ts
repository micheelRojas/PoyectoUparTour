import { FiltroTransportePipe } from './filtro-transporte.pipe';

describe('FiltroTransportePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTransportePipe();
    expect(pipe).toBeTruthy();
  });
});
